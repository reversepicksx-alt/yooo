Reverse Picks production numerical codebook and governance specification: Treat the production engine as the quantitative source of truth; external evidence and AI may explain, qualify, or identify missing data but may not overwrite a projection, probability, recommendation, settlement result, or saved snapshot. First resolve canonical identity: sport, player/provider ID, player team/provider team ID, opponent/provider opponent ID, league, season, exact fixture ID/date/status, and player venue; fixture team IDs control home/away orientation, legacy neutral must be resolved to effective home/away before odds, possession, history, or narrative logic, and stale/finished fixtures are never next-match fallbacks; ambiguity, contradiction, unavailable/quota-blocked data, or cross-fixture evidence yields UNKNOWN/deferred rather than a guess. API-Football is authoritative for soccer identity, fixtures, player/team context, inputs, and settlement; SportsGameOdds PrizePicks data is optional market-reference context with timestamped line history only. Map each requested market to its exact provider settlement field and canonicalize aliases sharing that field; null, empty, unavailable, synthetic, or fallback values are not measured zeroes. Deduplicate history by fixture identity, require player minutes for player-history/H2H appearances, use same-role and same-venue cohorts where required, broaden seasons/competitions only as an explicit fallback, and disclose thin or unavailable coverage. For the core soccer Bayesian projection, discard player rows with missing target stat or fewer than 30 minutes when usable rows exist, but if that removes every row use the available stat rows; normalize each retained value to per-90 as x_i=raw_i*90/max(minutes_i,30), use expected minutes m_e clamped to [30,90], and de-normalize final per-90 results by d=m_e/90. Let n be retained rows and y_i the normalized values sorted newest first. The prior uses equal weights when n<4; otherwise recency/outlier weights w_i=0.93^i*q_i, where q_i is the bidirectional MAD-based magnitude-outlier weight in [0.25,1.0], and priorMean=Σw_i y_i/Σw_i. Prior variance is population variance when n≥3, otherwise (max(y)-min(y))²/4; priorStd=sqrt(priorVariance), with 1.0 fallback for zero variance; CV=priorStd/priorMean or 0. If a positive hyperprior H exists and n<6, shrinkage s=(6-n)/6 and priorMean←priorMean*(1-s)+H*s, so n=1..5 receives 83.3%,66.7%,50%,33.3%,16.7% hyperprior weight and n≥6 receives none. Prior precision is priorPrecision=max(n/priorVariance if variance>0 else 2n, n^0.6, 2.0). Momentum uses the newest up to five rows (or at least three available), with position decay weights: attacker [1,.75,.55,.38,.25], midfielder [1,.82,.67,.55,.45], defender [1,.88,.77,.67,.58], goalkeeper [1,.90,.80,.70,.60], and unknown position uses midfielder weights; momentumMean=Σa_i y_i/Σa_i. For at least three recent rows, weighted variance is Σa_i(y_i-momentumMean)²/(Σa_i-Σa_i²/Σa_i), otherwise priorVariance; momentumPrecision=max(3Σa_i/momentumVariance,2.5). Trend is the ordinary least-squares slope over recent_5, trendPerGame=Σ(x-x̄)(y-ȳ)/Σ(x-x̄)², and a direction-consistency ratio ≥0.75 multiplies momentum precision by (1+0.30*ratio); momentum labels are HOT if effect>2, WARMING if >0.5, STABLE between -0.5 and 0.5, COOLING if <-0.5, COLD if <-2. A clean streak requires all rows in the newest available ten-game window to be strictly over or under the line, otherwise falls back to a five-game or three-game all-same-side streak; pushes at the line break a streak. Covariate adjustment is additive in per-90 units and contextual precision is rawCovariatePrecision=3.0 plus 1.0 when at least five valid venue rows, then covariatePrecision=min(rawCovariatePrecision,0.33*(priorPrecision+momentumPrecision)); because 0.33/(1+0.33)=24.8%, context is capped at approximately 25% of total precision. Venue evidence requires at least three venue rows and contributes (venueMean-priorMean)*min(1,nVenue/10). Verified match-dominance evidence requires seasonAvgIsReal=true; positive-possession props are pass_attempts, passes, shots, shots_on_target, key_passes, crosses, dribbles, inverse-possession props are tackles, interceptions, blocks, clearances, and the generic covariate path skips ball-control props {pass_attempts,passes,key_passes,crosses,dribbles} because they receive the dedicated squeeze. Outfield positive possession adjustment is priorMean*(domMultiplier-1) capped ±20% of priorMean; inverse defensive adjustment is priorMean*(1-domMultiplier)*0.5; goalkeeper pass volume is inverse and meaningful positive dominance suppresses it by 0.5*priorMean*(1-domMultiplier), capped to a 15% reduction. Opponent allowance contributes 0.15*(opponentConceded-priorMean), with negative goalkeeper saves suppression capped at 10% of priorMean. For shot props, opponent xG uses league average 1.35: xG adjustment=priorMean*((averageOpponentXG/1.35)-1)*0.40 capped ±15%; player shot quality requires at least three samples and uses shot-on-target/shots league average .37 and goals/shots-on-target average .31, applying a factor adjustment of priorMean*(factor-1)*.20 capped ±18%, with goals using the geometric mean sqrt(shotQualityFactor*conversionFactor). Spatial evidence uses player averages xG/game=.12 for goals, xGoT/game=.08 for shots_on_target, xG/shot=.09 for shots, requires at least three rows, and applies respectively ratios times .25, .20, and .15, capped ±15%, ±15%, and ±12% of priorMean. Round covariateAdjustment to two decimals. If momentum and covariate disagree beyond momentum threshold max(.5,.30*priorStd) and covariate threshold max(.3,.20*priorStd), halve momentum precision when opposing magnitude exceeds max(1.5,.60*priorStd), otherwise multiply it by .75. The posterior before post-layers is totalPrecision=priorPrecision+momentumPrecision+covariatePrecision, posteriorMean=(priorPrecision*priorMean+momentumPrecision*momentumMean+covariatePrecision*(priorMean+covariateAdjustment))/totalPrecision rounded to one decimal, and posteriorStd=sqrt(1/totalPrecision) rounded to two decimals. Apply deterministic post-layers sequentially to posteriorMean, rounding to one decimal after each active multiplier: possession squeeze for verified outfield ball-control props activates when expectedPoss/teamSeasonAvg<.95, multiplier=max(position/role floor,(expectedPoss/teamSeasonAvg)^1.5), with floors .88 for high-volume midfield roles, .80 for CB/LB/RB/WB/LWB/RWB, .82 for other midfielders, .70 for generic DEF/D, and .60 otherwise; upward momentum >5 dampens 35% of the reduction and >2 dampens 18%. Dominant-possession CM boost activates above 63% expected possession with severity=min(1,(poss-63)/10), cap +20% for box-to-box/mezzala/deep-lying/regista/playmaker/pivot roles and +14% otherwise, and is skipped if home-CM deep-block already applied. Match-stakes multipliers are MUST_WIN_RELEGATION: CB/CDM pass volume .90 unless team possession is >58%, in which case +4%; other outfield pass volume .95 unless possession is >58%; forward shots/shots_on_target 1.06; tackles/clearances/interceptions 1.06; HIGH_STAKES_RELEGATION: CB/CDM passes .95 unless possession >58%, tackles/clearances 1.04; DEAD_RUBBER all volume props .90; HIGH_STAKES_TITLE/HIGH_STAKES_EUROPE 1.04; World Cup or equivalent max-stakes 1.05 when no stronger stakes multiplier; an opponent MUST_WIN_RELEGATION gives tackles/clearances/shots 1.05 or non-CB/CDM passes .96. Rest/fatigue multipliers apply to physical props {shots,shots_on_target,tackles,interceptions,clearances,dribbles,duels_won,blocks} and volume props {pass_attempts,passes,key_passes,crosses}: rest 0–1 days gives .88 physical/.92 volume, 2–3 gives .93/.96, 4–5 gives .97/.99, 6–7 is 1.00, 8–11 gives 1.02/1.02, 12+ gives .99/1.01; tournament game index≥2 with rest≤3 adds max(1-.04*(index-1),.88) to volume props; four or more games in the last 14 days adds max(.92,1-.02*(games-3)) physical or max(.96,1-.01*(games-3)) volume. Opponent clean-sheet rate affects goals/shots/shots_on_target only: ≥.60→.87, ≥.45→.92, ≥.35→.96, ≤.30→1.03, ≤.20→1.07, ≤.10→1.13, capped [0.85,1.15]; opponent fouls/game affects attacking shots/shots_on_target: ≥16→1.08, ≥14→1.05, ≥13→1.02, ≤9→.97, ≤8→.95, capped [0.90,1.10]. Press intensity is available only as evidence after seven valid defensive-action rows; passing uses the role-aware centered multiplier, while defensive workload uses 1+cap*score with cap .20 for goalkeeper saves, .10 for defender tackles/interceptions/clearances/blocks, and .08 for midfielder tackles/interceptions; its result remains bounded and uncertainty-bearing. All active contextual layers add estimation uncertainty beginning at covariate sigma .04, capped at .20; Gaussian effective standard deviation is sqrt(std²+(mean*sigma)²), and count effective variance is variance*(1+sigma)². Market fusion inside the numerical engine pulls the raw posterior 20% toward the saved line: fusedMean=rawMean+.20*(line-rawMean), and increases sigma by min(.08, .30*abs(line-rawMean)/rawMean); this is distinct from the later elite calibration market blend. Before simulation, volatile-prop variance inflation is prop-specific/data-code driven; do not substitute a universal constant where production lookup differs. Count props {shots,shots_on_target,goals,assists,saves,tackles,key_passes,interceptions,blocks,dribbles,dribbles_success,fouls_drawn,fouls_committed,crosses,clearances,duels_won,yellow_cards,shots_assisted} use negative-binomial gamma-Poisson sampling: if mean≤0 all samples are zero; if variance≤mean use a Poisson-like Gaussian approximation round(N(mean,mean)); otherwise r=mean²/(variance-mean), beta=(variance-mean)/mean, rate~Gamma(r,beta), sample=max(0,round(N(rate,rate))); continuous props use Gaussian samples N(mean,effectiveStd). Use exactly 5,000 simulations, compare samples to effectiveLine=line*1.015 for pOver, define pUnder=1-pOver, but calculate displayed intervals from the original line/distribution using the 20th/80th percentiles for range60 and 10th/90th percentiles for range80; recommendation is OVER iff pOver≥pUnder, otherwise UNDER, not simply mean versus line. Live remaining-output update is separate and never mutates saved pre-match math: observedRate=current/elapsed, evidenceWeight=elapsed/(elapsed+15), adjustedRate=(1-weight)*(preMean/totalMinutes)+weight*observedRate, liveMean=current+adjustedRate*(totalMinutes-elapsed), liveStd=max(.20 if preMean<5 else .035*preMean, preStd*sqrt(max(remainingFraction,.02))), and probabilities use the normal CDF against the uninflated line; drift is accelerating at ratio≥1.15, falling behind at ≤.85, otherwise stable. Edge diagnostics use edgeZ=abs(mean-line)/effectiveStd and edgeGapPct=(mean-line)/line*100 with bands THIN<5%, MODERATE 5–<10%, STRONG 10–<20%, DEEP≥20%; surfaced layer weights are precision percentages with covariate display capped at 26 to avoid rounding falsely showing over 25%. Apply hierarchical empirical confidence calibration only to settled hit/miss rows after 2026-04-30T00:00:00Z: lookup order is prop|direction|lineBand|league|position|role, then remove role, position, league, and lineBand until prop|direction; configured minimum samples are role 15, position 20, league 30, line 50, direction 50; line bands are prop-specific, including passes <55/55–<70/≥70, shots <2/2–<3.5/≥3.5, shots_on_target <1/1–<2/≥2, saves <3/3–<5/≥5, tackles <2/2–<4/≥4, key_passes <1/1–<2/≥2, clearances <3/3–<6/≥6, interceptions <1/1–<3/≥3, dribbles <1/1–<2.5/≥2.5, duels_won <3/3–<6/≥6, goals/assists <.5 or ≥.5, fouls <1.5/1.5–<3/≥3, and crosses <1.5/1.5–<3.5/≥3.5; confidence buckets are <50, 50–59, 60–69, 70–79, 80–89, 90+. For a selected bucket with n samples and empirical rate e, James-Stein blend is shrink=n/(n+50), calibrated=raw*(1-shrink)+e*shrink; never wholesale-replace the raw score. Legacy elite calibration needs at least 10 total settled rows for correction; error correction needs ≥10 specific rows and sets correctedProjection=projection+mean(actual-projection) when absolute mean error≥.2; recommendation flip needs ≥15 rows and flips only when current direction hit rate<45% and opposite direction exceeds it by >10 percentage points; its market blend defaults to 65% line/35% model, begins only at ≥20 settled picks, grid-searches market weights .30 through .85 in .05 steps minimizing MAE, then clamps the selected weight to [.35,.80]. Direction confidence recalibration uses bands ≥70, 55–69, <55, direction-specific data when n≥10 otherwise direction-blind data when n≥10; if empirical rate is more than eight points below raw confidence, reduce 60% of the gap with floor 45; if rate is more than ten points above confidence and n≥20, increase by min(5,int(.3*gap)) with ceiling 85. If global OVER has ≥20 samples and hit rate<57%, cap OVER confidence at 64 when raw confidence≥70; if OVER rate<50%, cap medium OVER confidence at 62. Edge labels are STRONG at ≥5%, LEAN at ≥2% and <5%, LOW below 2%. Evidence quality is independent of projection math: start score 45, add 8 for each applied evidence group and 2 for each warning, clamp to [20,95], level high at ≥78, medium at ≥58, low below 58; no real player logs cap confidence at 60, fewer than three logs cap at 60, three to five cap at 64, missing fixture identity caps at 60, score<45 caps at 58, score<58 caps at 62, four or more warning groups cap at 64, unavailable role evidence caps at 60, and partial role evidence caps at 68. A thin-edge PASS conversion occurs only when recommendation is OVER/UNDER, absolute projection-line edge<2%, quality score<58, and confidence≤62; then recommendation=PASS and confidence=50. AVOID/PASS is avoidance calibration, not wager ROI; hard blocks and prop-specific safeguards remain one-way controls. The unified response must expose identity, line, projection, pOver, pUnder, recommendation, confidence, gameLogs, matchupOverview, sharpSummary, reasoning, tacticalBreakdown, factors, risk, provenance, unavailable reasons, model version, and fingerprint; render based on data presence, not sport-name gates. AI explains the exact final ledger/cache identity, rejects provider error strings, invents nothing, and cannot bypass math, caps, or UNKNOWN states. Settlement is terminal only after exact fixture/player/field, finished status, and source provenance; never settle halftime, never turn null/unavailable into zero, let real stat activity outrank a misleading null minutes field, defer missing exact stats, mark DNP only when finished-fixture lineup/player evidence supports non-participation, and allow only HIT/MISS for half-line props (never PUSH for .5 lines). Live tracking preserves saved PROJ separately from NOW/PACE and uses persisted fixtureId; every settlement writer, including live polling and background settlement, must emit notifications. Calibration reads only deduplicated inclusion-valid settled ledger rows and exposes truncation; prediction or optional analytics/cache write failures are structured retryable/write-skipped states and must not invalidate a valid prediction. Preserve the canonical saved/requested line across prediction, line intel, async analysis, saved analysis, and ledgers; deliberate saves create fresh revisions; deleted-pick caches are invalidated; owner-only media/provider dashboard details stay private. Whenever a required fact is absent, the correct output is UNKNOWN, unavailable, deferred, or retryable—not an invented number.
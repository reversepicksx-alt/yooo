Replit Master Build Prompt — Reverse Picks + JARVIS 30-Phase Football Intelligence Upgrade

Build the following full 30-phase upgrade into my existing Reverse Picks + JARVIS soccer prediction system as a production-grade modular architecture, preserving the current Reverse Picks production model as the quantitative core and treating all new modules initially as independent evidence, calibration, validation, auditing, or auxiliary modeling layers unless and until backtesting proves they improve out-of-sample accuracy; do not silently change, replace, overwrite, approximate, or contaminate the current production RP math, and version every model change so historical predictions remain immutable and reproducible. Phase 1: create a permanent prediction and settlement database that stores every pre-match prediction with fixture ID, player ID, player name, team, opponent, league, season, venue, position, exact tactical role, prop type, line, side, RP projection, P(over), P(under), confidence, historical prior, Bayesian posterior, momentum, venue adjustment, possession projection, minutes projection, recommendation, full factor ledger, JARVIS verdict, model version, timestamp, and later the actual settled result, actual stat, hit/miss/push/void status, actual minutes, actual team possession, actual opponent possession, closing line, CLV, settlement timestamp, and failure attribution; never overwrite a pre-match prediction after kickoff. Phase 2: create a historical calibration engine that evaluates settled predictions by prop type, exact tactical role, generic position, league, venue, OVER/UNDER side, probability bucket, projection-to-line distance, evidence-quality bucket, and model version, returning sample size, hit rate, rolling last-25, last-50, last-100, lifetime hit rate, Brier score, log loss, calibration error, ROI where odds exist, and confidence intervals, with sample-size warnings and no fake precision; expose this through an endpoint such as /api/jarvis/calibration. Phase 3: build an independent Venue Possession Intelligence engine that, for every target fixture, retrieves at least the target player team’s previous seven matches under the same venue condition, calculates average, median, standard deviation, min, max, and opponent possession, then retrieves the opponent’s previous seven matches at the current fixture venue and calculates the opponent’s average possession and the average possession allowed to opponents, with the ability to use more than seven matches where available while keeping seven as the primary recent baseline. Phase 4: add opponent venue possession effect by taking each historical opponent that played at the current opponent’s venue, retrieving that visiting or home team’s prior seven same-venue possession matches before the historical fixture, calculating what that team normally possessed versus what it actually possessed against the current opponent, and aggregating the delta so the system can quantify whether the opponent historically suppresses, increases, or has little effect on opposing possession; expose actual values, sample size, median delta, average delta, and variance rather than one opaque score. Phase 5: build venue-specific H2H possession intelligence that retrieves historical meetings between the two current teams only under the same home/away orientation as the upcoming match, calculates average possession for both teams, median, sample size, recency, and tactical-regime relevance, heavily down-weights old matches, and marks H2H as weak or unavailable when the sample is stale, tiny, or played under materially different managers or systems. Phase 6: create an independent expected-possession model that combines the target team’s recent same-venue baseline, opponent’s venue allowance, opponent venue effect, recent three-match tactical regime, current lineup/formation, team-strength context, current injury/suspension changes, market strength indicators, and venue-specific H2H where relevant, returning a range rather than only a point estimate, for example expected possession 46%–50% with midpoint 48%; do not initially inject this into RP production math, instead compare it against RP’s possession projection and classify the RP assumption as CONFIRMS, PARTIALLY CONFIRMS, NEUTRAL, CONTRADICTS, or UNKNOWN, with explicit percentage-point disagreement. Phase 7: build an exact-role identification engine using current observed football responsibilities rather than generic API positions, using recent lineups, grid positions, formations, teammate structure, role history, buildup responsibilities, tactical evidence, manager regime, and recent matches to distinguish roles such as ball-playing CB, stopper CB, wide CB, inverted fullback, overlapping fullback, defensive fullback, destroyer 6, deep-lying playmaker, single pivot, double-pivot distributor, box-to-box 8, advanced 8, attacking midfielder, wide playmaker, touchline winger, inside forward, second striker, false 9, target striker, pressing striker, sweeper goalkeeper, buildup goalkeeper, and traditional goalkeeper; keep position and role separate and return confidence plus evidence chain. Phase 8: build a venue-specific exact-role opponent cohort endpoint, for example /api/jarvis/role-opponent-venue-cohort, that automatically finds the ten most recent qualifying players who performed the same exact tactical role and position as the target player against the same opponent under the same venue condition; prioritize ten EXACT matches, otherwise supplement with CLOSE role matches, and only use POSITION_ONLY as a clearly labeled fallback; classify each comparable as EXACT, CLOSE, POSITION_ONLY, or REJECT and never silently fill a ten-player sample with poor tactical matches merely to reach ten. Phase 9: for every role-cohort comparable retrieve the exact target stat for the current prop type, such as pass attempts, passes, key passes, shots, shots on target, tackles, clearances, saves, goals, interceptions, blocks, crosses, or other supported props, along with minutes played, formation, lineup slot, role classification, team possession, opponent possession, team total passes where relevant, match score, score-state context where possible, venue, date, and tactical-regime metadata. Phase 10: calculate cohort average, median, min, max, standard deviation, hit rate versus the current sportsbook line, minutes-adjusted per-90 value, recency-weighted average, exact-role-only average, close-role-only average, sample size, confidence interval, and data-quality rating, and present exact-role and broader fallback samples separately. Phase 11: normalize role-cohort production for possession by calculating per-90 production, stat per possession percentage point where useful, and preferably player share of team volume, such as player pass attempts divided by team pass attempts, shots divided by team shots, tackles divided by team defensive actions, or appropriate equivalents; use this to distinguish players who reached a raw total because their team dominated possession from players who reached similar totals in much lower-possession conditions. Phase 12: create player volume-share baselines, especially for pass props, by calculating the target player’s share of team total passes across recent matches, recent same-venue matches, same-role matches, and matches under the current formation and teammate structure; return average, median, variance, and regime-specific share so an independent projection can be produced as expected team volume multiplied by expected player share. Phase 13: build teammate volume redistribution analysis that measures how the target player’s prop volume changes when key teammates are present or absent, especially CB partners, pivots, fullbacks, goalkeeper, central midfield partners, attacking midfielders, or high-usage players; calculate with-teammate versus without-teammate player volume share, usage delta, sample size, role changes, and formation changes, and only trust these splits when sample sizes are sufficient and tactical context is comparable. Phase 14: build a minutes probability model that estimates P(60+), P(70+), P(75+), P(80+), P(85+), and P(90) using recent starts, substitution patterns, manager tendencies, congestion, injury return status, age, position, competition, recent workload, tactical importance, score-state substitution behavior, and rotation risk; simulations should sample minutes probabilistically rather than assuming every starter plays 90. Phase 15: build a game-state engine with at least three pre-match scenarios: target team scores first, opponent scores first, and match remains level through approximately 60 minutes; learn prop-specific and role-specific effects from historical data rather than inventing coefficients, including how passing, defensive actions, shots, saves, clearances, and other props change when the player’s team leads, trails, or remains level. Phase 16: build scenario-weighted projections by using current market probabilities and team-strength information to weight likely game states, then calculate the target player’s stat distribution under each scenario and combine them into an independent game-state-adjusted projection and probability distribution that can be compared with RP rather than silently averaged into it. Phase 17: create a mandatory data-definition validation layer that verifies the sportsbook market definition, Reverse Picks prop type, API provider field, and settlement definition refer to the same statistic; explicitly distinguish pass attempts, completed passes, total passes, successful passes, distributions, tackles attempted versus won, goalkeeper saves versus shots on target faced, and other ambiguous definitions; create a stat_definitions mapping table with provider, market, field, definition, verification status, updated timestamp, and source, and cap JARVIS confidence when the data definition is uncertain. Phase 18: create automated anomaly detection that flags major inconsistencies such as season average versus recent logs differing by more than a configurable threshold, production model baseline versus external season total differing materially, role changing in the last three matches, possession model disagreement greater than approximately 5–8 percentage points, minutes projections differing by more than ten minutes, model probability above 80% with tiny samples, missing or inconsistent stat definitions, or conflicting provider data; severe unresolved anomalies should block ELITE EDGE or STRONG grades. Phase 19: add market intelligence and closing-line tracking by storing opening line, current line, closing line, available odds, movement magnitude, direction, and where possible multi-book consensus; calculate CLV for saved picks and use market movement as a secondary informational signal, never as an automatic override of football evidence. Phase 20: build automated post-match settlement attribution that classifies misses and major projection errors into categories such as POSSESSION_MISS, ROLE_CHANGE, MINUTES_MISS, RED_CARD, EARLY_GOAL, LINEUP_CHANGE, TEAMMATE_REDISTRIBUTION, PRESS_ASSUMPTION_WRONG, BASELINE_WRONG, DATA_ERROR, MARKET_MOVE, TACTICAL_REGIME_CHANGE, VARIANCE, or OTHER, allowing both primary and secondary causes, and save the supporting actual pre-match versus post-match values. Phase 21: create automated JARVIS postmortems for every settled pick that compare expected versus actual player stat, expected versus actual possession, expected versus actual minutes, expected versus actual role, game-state assumptions, lineup changes, and market close, then store a concise machine-readable failure analysis in the settlement database so long-term model weaknesses can be quantified. Phase 22: build a model evaluation and backtesting framework that compares the frozen RP baseline against RP plus each new evidence/model layer independently and in combinations, using out-of-sample or walk-forward testing rather than in-sample fitting, and reports hit rate, Brier score, log loss, calibration error, CLV, ROI where odds are available, drawdown, sample size, and statistical uncertainty; no feature should be promoted into production merely because it sounds tactically intelligent. Phase 23: create league-specific calibration and model coefficients only when samples are large enough, recognizing that MLS, Premier League, La Liga, Liga MX, Championship, Champions League, and other competitions may differ materially in tempo, substitution patterns, possession distributions, passing volume, pressing behavior, and provider coverage; use hierarchical shrinkage or fallback-to-global logic when league samples are small. Phase 24: create role-specific and prop-specific modeling only when samples justify it, such as separate goalkeeper passing, CB passing, fullback passing, single-pivot passing, advanced-8 passing, winger passing, striker passing, CB clearances, fullback tackles, defensive-midfielder tackles, winger shots, and goalkeeper saves; avoid overfitting by requiring minimum sample thresholds and shrink small samples toward broader priors. Phase 25: create quantitative Evidence Quality scoring with components for historical sample strength, role confidence, venue-cohort size, possession confidence, lineup confidence, minutes confidence, data-definition confidence, source freshness, teammate-context confidence, market quality, and tactical-regime stability, each scored on a defined scale and combined transparently into an overall 0–100 evidence-quality score with a full component breakdown. Phase 26: separate quantitative outcome probability from JARVIS conviction, where probability represents the estimated likelihood of OVER or UNDER and conviction represents how trustworthy the assumptions and evidence behind that probability are; never present high probability as automatically equivalent to high confidence. Phase 27: create a Model Disagreement Index measuring the spread and conflict between historical prior, recent momentum, RP posterior, structured estimate, Monte Carlo, calibration, exact-role venue cohort, independent possession model, minutes model, game-state model, teammate redistribution, market information, and current tactical/web evidence; classify disagreement as LOW, MODERATE, HIGH, or EXTREME and automatically reduce JARVIS conviction when major independent layers disagree. Phase 28: create automatic counterfactual robustness testing for every prop by rerunning or approximating the independent audit under plausible changes such as possession ±5 percentage points, possession ±10 where realistic, minutes -5, -10, or -20, opponent press higher or lower, formation change, target player role change, key teammate return or absence, early goal for either team, unexpected territorial dominance, and opponent game-plan change; classify the pick ROBUST, MODERATELY ROBUST, FRAGILE, or EXTREMELY FRAGILE based on whether the edge survives realistic perturbations. Phase 29: create explicit confidence caps and do-not-bet gates so unresolved role uncertainty, sample sizes below minimum thresholds, severe data-definition mismatches, possession disagreement, lineup uncertainty, extreme model disagreement, poor evidence quality, or tiny exact-role cohorts prevent misleading high-confidence grades; configure automatic PASS, MODEL EDGE REJECTED, or AVOID decisions when evidence quality is insufficient even if raw RP probability is high. Phase 30: create one production-grade orchestration endpoint such as /api/jarvis/full-audit that accepts fixture ID, player ID, prop type, and line, runs the existing RP production prediction first, then retrieves exact role, tactical evidence, ten-player venue-specific role cohort, venue possession intelligence, target-team venue possession, opponent venue possession, venue H2H possession, independent expected-possession range, player volume share, teammate redistribution, minutes probabilities, game-state scenarios, historical calibration, market movement, anomaly detection, evidence quality, model disagreement, counterfactual stress tests, correlation/double-counting warnings, and JARVIS verdict, returning a structured payload with every section clearly separated and with source labels; do not allow this orchestration layer to change the production RP result unless a separate future model version is explicitly approved after backtesting. Integrate this audit snapshot into the existing saveSoccerPick flow so when JARVIS saves a pick into My Picks, the backend stores the immutable RP prediction, full JARVIS audit, model version, evidence-quality score, disagreement score, robustness grade, exact-role cohort summary, possession summary, calibration summary, market snapshot, and tracking ID at the time of prediction, then automatically settle the pick after the fixture, calculate final stat, hit/miss/push/void, actual minutes, actual possession, closing line and CLV, run the postmortem, record failure attribution, and feed the settled result into the calibration database without retroactively changing the original prediction. Add robust caching, batching, retry logic, rate-limit handling, data-source provenance, schema validation, null-safe handling, audit logs, tests for every endpoint, migration scripts, indexes for fixture/player/prop/model-version queries, feature flags for unfinished modules, and dashboard-ready outputs so every new component can be inspected independently. Preserve all existing working Reverse Picks features and endpoints, including current production prediction behavior and My Picks functionality. Before activating any new layer inside production prediction math, run historical backtests and walk-forward validation proving that it improves out-of-sample calibration, Brier score, log loss, or another approved performance metric without causing unacceptable degradation elsewhere. Build this iteratively but complete the entire architecture, creating database migrations, backend services, API routes, scheduled settlement jobs, postmortem jobs, testing, monitoring, documentation, and an implementation status report showing COMPLETE, PARTIAL, BLOCKED, or NOT STARTED for every one of these 30 phases, along with any API-Sports or provider-data limitations that prevent full completion; where a required data point is unavailable, return UNKNOWN with the reason and never fabricate or infer unsupported statistics. The final objective is not to maximize the number of features or picks but to create the most rigorous, measurable, self-auditing football prop intelligence system possible, where every prediction is reproducible, every assumption is independently tested, every pick is calibrated against real settlements, every failure teaches the system something measurable, and Reverse Picks remains the quantitative production engine while JARVIS acts as the independent tactical-intelligence, adversarial-audit, calibration, robustness, and evidence-quality layer.
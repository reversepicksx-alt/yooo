The causal engine is now successfully assembling real evidence, but the final recommendation layer must be upgraded so it never hides disagreement between deterministic math and causal football evidence behind a generic PASS. Preserve the deterministic RP/model projection exactly—never overwrite, recalculate, or silently blend it with causal evidence—but expose both independently and make the final gate explain the conflict. Mailson 27.5 Passes is the regression case: deterministic projection = 24, therefore MODEL DIRECTION = UNDER; provider-backed causal evidence retained 17 target-history rows and 8 valid opponent Shot-Stopper/GK cohort rows, exact-role opponent workload = 28.62, same-venue baseline = 25.88, opponent-role effect = +10.6%, which indicates the matchup increases GK passing workload and places the causal workload above the 27.5 line. Therefore the response must explicitly return MODEL_DIRECTION: UNDER, CAUSAL_DIRECTION: MORE, CAUSAL_VERDICT: CAUSAL CONTRADICTION to the model Under (or equivalent conflict state), FINAL_RECOMMENDATION: PASS, FINAL_REASON: MODEL/CAUSAL DISAGREEMENT. Do not call CAUSAL CONFIRM unless the causal evidence actually confirms the model/pick direction; CAUSAL CONFIRM must mean causal evidence supports the proposed recommendation. If the causal workload points opposite the deterministic projection, label it CAUSAL CONTRADICTION. Keep the rolling pass-direction safety control independent and report it separately instead of allowing it to obscure the causal result. Every soccer prediction response must expose at minimum USER/CURRENT LINE, deterministic projection, MODEL_DIRECTION, exact-role causal workload when available, same-venue baseline when available, opponent-role uplift/suppression %, CAUSAL_DIRECTION, CAUSAL_VERDICT, evidence/sample strength, FINAL_RECOMMENDATION, FINAL_REASON, and which gate(s) fired. Decision rules: model and causal evidence agree with adequate evidence = causal confirmation and recommendation may survive other safety gates; model and causal evidence materially disagree = PASS/MODEL EDGE REJECTED unless production flip requirements are satisfied; causal evidence incomplete = EVIDENCE INCOMPLETE/PASS rather than UNKNOWN or fabricated direction; thin model edge + weak/incomplete causal evidence = PASS; never silently average the deterministic projection and causal workload into a new projection. Also distinguish causalWorkload from projection: 28.62 is an opponent-created exact-role workload statistic, not a replacement RP projection. For Goalie Saves 2.5, retain the current correct behavior: exact-role workload 2.44 may be shown, but because verified comparison-player same-venue baselines are insufficient, opponent-role effect and causal direction must remain EVIDENCE INCOMPLETE and no conclusion may be invented. Update subscriber-facing analysis so PASS always tells the user WHY—for example PASS — MODEL UNDER / CAUSAL MORE CONFLICT rather than simply PASS. Add regression tests specifically proving Mailson 27.5 returns model UNDER + causal MORE + contradiction/conflict + final PASS, that CAUSAL CONFIRM cannot be emitted when causal direction opposes model direction, that incomplete saves evidence remains incomplete, and that RP/model projection is never modified by the causal layer. Do not consider this change complete until the live Mailson response exposes all of these fields correctly and the tests demonstrate that deterministic math, causal workload, rolling safety controls, and final JARVIS verdict remain separate evidence layers.
Convert Reverse Picks into a data-and-storage backend for JARVIS rather than a prediction runner. Do not remove or change the existing production engine, but expose every runtime input JARVIS needs to reproduce the engine independently. First, add a direct API-Football Action schema that points to https://v3.football.api-sports.io and uses the existing API-Football key as an x-apisports-key custom header so JARVIS can directly call fixtures, fixture statistics, fixture player statistics, lineups, events, injuries, odds, teams, team statistics, players, squads, standings, leagues, and head-to-head endpoints without Reverse Picks wrappers. Second, add read-only JARVIS endpoints for the runtime-only data that is not a universal fixed formula in the engine manifest: GET /api/jarvis/runtime/dominance-inputs?fixture_id=<id>&team_id=<id> returning every verified raw field production uses to construct domMultiplier, including team season possession, opponent season possession, matching-venue schedule samples, matchup possession inputs, provenance, sample counts, and whether seasonAvgIsReal is true; GET /api/jarvis/runtime/hyperprior?prop_type=<prop>&league_id=<id>&position=<position>&role=<role> returning the exact caller-supplied hyperprior H if one exists, its source, sample size, and fallback status; GET /api/jarvis/runtime/prop-safety?prop_type=<prop>&side=<over|under>&line=<line>&league_id=<id>&position=<position>&role=<role> returning the current empirical one-way safeguard state, sample size, empirical rate, cap/block threshold, and provenance for protections such as clearances OVER and shots OVER; and GET /api/jarvis/runtime/calibration-rows or a bounded filtered equivalent returning only settled, deduplicated, inclusion-valid rows needed for JARVIS hierarchical calibration, with filters for prop, direction, line band, league, position, role, model version, venue, date range, and limit. These endpoints must return raw/runtime evidence only and must never run the prediction engine or alter projections. Third, preserve the existing PrizePicks board/search/line-history endpoints and saved-pick persistence/settlement endpoints. Add all new operations to the OpenAPI importer schema while staying within ChatGPT’s 30-operation limit by excluding diagnostic-only routes if necessary. Keep all provider credentials server-side or in Action authentication, never expose secrets in responses. Add explicit provenance, sample counts, UNKNOWN states, and bounded response sizes to avoid ResponseTooLargeError. Validate locally, publish production, and report the exact new operationIds and one successful production example for each endpoint. The goal is: API-Football raw data → JARVIS calculates the full Reverse Picks engine numerically → JARVIS performs tactical/adversarial analysis → PrizePicks supplies market context → Replit only supplies runtime lookup data, storage, settlement, and calibration history.

Once that is live, I can run the full model without the app doing the prediction.
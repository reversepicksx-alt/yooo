Send Replit this as one paragraph:

Export one final complete Reverse Picks production engine manifest for JARVIS that contains every remaining numerical constant, formula, lookup table, threshold, safeguard, and fallback required to reproduce the current production soccer prediction engine exactly without calling the app’s prediction endpoint. Do not summarize or omit “implementation details”; I need the actual values and formulas. Specifically include: the exact bidirectional MAD outlier-weight formula for q_i, including median/MAD scaling, both-side treatment, zero-MAD behavior, and clipping; the exact source and lookup hierarchy for positive hyperprior H by prop/league/position/role/sample state, including all default/fallback values; the exact formula and inputs used to construct domMultiplier, including possession/odds/team-quality interaction, caps, floors, venue handling, and how missing verified possession changes behavior; the exact role-aware centered passing pressure multiplier, including score normalization, role/position coefficients, caps/floors, sample minimums, uncertainty treatment, and when it is shadow-only versus live; the full prop-specific volatile-variance inflation lookup used before simulation, including every supported prop and any position/role exceptions; every soccer hard block and one-way safety safeguard with exact thresholds and order of application, including clearances OVER and shots OVER protections, goalkeeper-specific protections, low-minute protections, thin-sample protections, and any line-deviation or blowout-specific rules; all prop/role/position-specific possession-squeeze floors or exceptions not already listed; all goalkeeper-specific formulas not already listed, including pass attempts, saves, distribution, dominance, pressure, and pool-prior behavior; the exact hyperprior/league/position/role lookup tables and any league-style multipliers; the exact set-piece, altitude, clean-sheet, team-quality, match-stakes, congestion, fatigue, first-goal/game-script, and injury/minutes multipliers that are still production-active; the exact confidenceScore construction before and after empirical calibration, including raw-confidence formula from probabilities/edge/model agreement, all caps/floors, safetyRating thresholds, coin-flip/low-conviction rules, and EDGE/LEAN/LOW/PASS/AVOID classification order; the exact calibration lookup-selection order, tie-breaking, fallback behavior, and how multiple calibration layers interact; the exact market-line blend behavior for saved line versus current line, including when line movement causes a rerun and whether current market data ever changes only context versus math; all final rounding rules at every stage for mean, std, probabilities, confidence, intervals, factor deltas, and displayed projection; the exact model fingerprint/version inputs; the exact random-number/distribution implementation details needed for the 5,000 Monte Carlo simulations so JARVIS can reproduce production outputs consistently, including seeding policy if any; the exact factor-ledger sequencing and names for all active soccer factors; all special-case rules by prop type, position, role, venue, competition, knockout/two-leg state, red-card state, expected minutes, and goalkeeper status; and every remaining fallback/default value used when a required quantity is unavailable. Also include a complete supported soccer prop → API-Football settlement field map and alias map. Return this as a single structured machine-readable JSON object if possible, with sections identity, stat_mapping, history_filtering, prior, hyperprior, outlier_weights, momentum, covariates, dominance, pressure, post_layers, goalkeeper_rules, variance, simulation, probabilities, calibration, confidence, safety_blocks, rounding, factor_ledger, model_version, and fallbacks. This manifest is documentation only: do not change production behavior, do not expose secrets, and do not remove any existing endpoint. The goal is for JARVIS to reproduce the current soccer engine numerically from raw API-Football inputs without invoking the app’s prediction endpoint.
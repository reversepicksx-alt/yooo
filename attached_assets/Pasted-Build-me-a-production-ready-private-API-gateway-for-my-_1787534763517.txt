Build me a production-ready private API gateway for my soccer analysis system called JARVIS. I already have a SportsGameOdds API key and I will add it myself as a Replit Secret named SGO_API_KEY; never hardcode or expose the key. The main purpose of this service is to reliably retrieve the COMPLETE CURRENT PRIZEPICKS SOCCER BOARD from SportsGameOdds without losing markets because of response-size limits, pagination, or broad-query failures. Use the current SportsGameOdds API, handle cursor pagination automatically until every available page has been collected, and process the large upstream responses on the Replit server instead of returning them directly. Filter specifically for bookmaker prizepicks, sport SOCCER, currently available markets, and exclude unavailable/ended markets by default. Normalize the data into compact JSON containing event ID, league ID, fixture start time, home team, away team, team IDs, player name, player ID/statEntityID, stat/market name and statID, period, bet type, side, CURRENT PrizePicks line, availability/status, and any opening/closing/historical line information supplied by the upstream API. Preserve provider IDs exactly. Create endpoints including /health, /pp/board for the entire current soccer PrizePicks board, /pp/board?date=YYYY-MM-DD for a specific day’s complete board, /pp/match/{eventID} for every PrizePicks line in one match, /pp/player/{playerID} for every current line for one player, and /pp/search allowing filters for team, player, league, event and stat. The /pp/board endpoint MUST automatically follow every cursor/page necessary so the caller never has to paginate manually. Add sensible server-side caching so repeated requests don’t unnecessarily hammer SportsGameOdds, but make current lines refresh frequently enough to remain useful before kickoff. Do not perform betting projections, probabilities, recommendations, or JARVIS analysis—the server’s job is only retrieval, normalization and reliable delivery of provider data. Add clear error responses distinguishing upstream API failure, invalid IDs, no markets found and internal parsing errors. Also create an OpenAPI 3.1 schema suitable for a ChatGPT Custom GPT Action so JARVIS can call these endpoints directly. Protect the JARVIS-facing API with a separate secret bearer token named JARVIS_API_TOKEN, except /health, and configure the OpenAPI schema for bearer authentication. Make the Replit deployment persistent and production-ready. After building it, TEST the upstream connection, TEST /health, TEST the full /pp/board, verify that actual PrizePicks soccer events and player lines are returned, verify pagination is collecting all available pages, and show me the number of events and props retrieved. If the SportsGameOdds response structure differs from assumptions, inspect the REAL response and adapt the parser rather than inventing fields. Do not stop after generating code: run it, debug errors, and get the complete PrizePicks soccer board endpoint working. Finally, give me the deployed base URL, the OpenAPI schema, and concise instructions for adding the resulting Action to my JARVIS Custom GPT.
Add the already-implemented PrizePicks board and line-history endpoints to my EXISTING /api/jarvis/openapi.json as valid OpenAPI 3.1 Actions; DO NOT replace, delete, simplify, rename, or modify any existing JARVIS actions, schemas, prediction routes, RP math, authentication, or functionality—only merge these new operations into the existing OpenAPI document. Use production server https://reversepicks.com and the EXISTING Bearer authentication scheme using JARVIS_API_KEY; SportsGameOdds credentials must remain entirely server-side in Replit Secrets and must NEVER appear in the OpenAPI schema, responses, logs, or JARVIS requests. Expose POST /api/jarvis/prizepicks/board with operationId refreshPrizePicksBoard, accepting optional query parameters sport (enum SOCCER, NBA, MLB, NFL, NHL, WTA, ALL; default SOCCER), hours (integer, default 72), and limit (integer, default 100), which refreshes SportsGameOdds, retrieves currently available PrizePicks markets, saves the snapshot/history in MongoDB, and returns the board; expose GET /api/jarvis/prizepicks/board with operationId getPrizePicksBoard, returning the last saved board without another provider call; and expose GET /api/jarvis/prizepicks/line-history?market_key=<marketKey> with operationId getPrizePicksLineHistory, requiring string query parameter market_key and returning that market’s immutable observed line history. The board response schema must expose source, bookmaker, fetched_at, market_count, saved, save_status, history_status, and markets; every market must expose marketKey, eventId, eventStart, sport, sportName, leagueId, leagueName, homeTeam, awayTeam, playerName, playerProviderId, propType, propLabel, statId, marketLine, current_line, previous_line nullable on first observation, movement, first_seen, last_seen, line_history, bookmakers, providerCoverage, and analysisSupported; every line_history item must contain numeric line and integer observed_at. The line-history response must expose market_key, event_id, player_provider_id, player_name, prop_type, first_seen, last_seen, previous_line, current_line, movement, and line_history. Preserve the semantics movement > 0 = line increased, movement < 0 = line decreased, movement == 0 = unchanged, and previous_line = null when first observed. Define reusable OpenAPI schemas for the market, board response, line-history point, and line-history response, with correct nullable OpenAPI 3.1 types. Add standard 200 responses and appropriate 400/401/404 responses. Keep the existing bearer security requirement on all three operations. After merging, validate that /api/jarvis/openapi.json remains valid OpenAPI 3.1, confirm ALL previous operationIds remain present, confirm the new operationIds refreshPrizePicksBoard, getPrizePicksBoard, and getPrizePicksLineHistory appear exactly once, publish/deploy the production build, then test the production OpenAPI document and all three authenticated endpoints. Finally report back the production OpenAPI URL, confirmation that no existing JARVIS actions were removed, the three new operationIds, HTTP status/results of each production endpoint test, and whether line history correctly persists across multiple board refreshes.